## build and deploy

每次编译完成后，执行以下操作：
1. 杀死正在运行的 `Game` 进程（`pkill -x Game`）
2. 复制 `build/app/Game` 到 `/home/daiaji/下载/レトリアの大冒険_URGE/Game`

也可以直接运行：
```
bash /home/daiaji/repo/urge/build_and_deploy.sh
```
